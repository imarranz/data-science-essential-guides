LIBRO="essential-guide-to-pandas"

pdftoppm -f 1 -l 1 -png "$LIBRO.pdf" "$LIBRO"
mv "$LIBRO"-*.png "$LIBRO".png

LIBRO="essential-guide-to-awk"

pdftoppm -f 1 -l 1 -png "$LIBRO.pdf" "$LIBRO"
mv "$LIBRO"-*.png "$LIBRO".png

LIBRO="essential-guide-to-seaborn"

pdftoppm -f 1 -l 1 -png "$LIBRO.pdf" "$LIBRO"
mv "$LIBRO"-*.png "$LIBRO".png

LIBRO="essential-guide-to-sql-with-python"

pdftoppm -f 1 -l 1 -png "$LIBRO.pdf" "$LIBRO"
mv "$LIBRO"-*.png "$LIBRO".png

LIBRO="essential-guide-to-scikit-learn"

pdftoppm -f 1 -l 1 -png "$LIBRO.pdf" "$LIBRO"
mv "$LIBRO"-*.png "$LIBRO".png

LIBRO="essential-guide-to-model-evaluation-metrics"

pdftoppm -f 1 -l 1 -png "$LIBRO.pdf" "$LIBRO"
mv "$LIBRO"-*.png "$LIBRO".png

LIBRO="essential-guide-to-clean-data"

pdftoppm -f 1 -l 1 -png "$LIBRO.pdf" "$LIBRO"
mv "$LIBRO"-*.png "$LIBRO".png

LIBRO="essential-guide-to-matplotlib"

pdftoppm -f 1 -l 1 -png "$LIBRO.pdf" "$LIBRO"
mv "$LIBRO"-*.png "$LIBRO".png

LIBRO="essential-guide-to-feature-engineering"

pdftoppm -f 1 -l 1 -png "$LIBRO.pdf" "$LIBRO"
mv "$LIBRO"-*.png "$LIBRO".png




